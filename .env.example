# Supabase (get from client's project: Supabase Dashboard → Settings → API)
# NEVER commit real credentials — use placeholders here, real values in .env only
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here